#pragma once

#include <stdbool.h>

// Вспомогательная функция для теста, которая создает массив из одинаковых элементов
void arraySame(int array[], int size);

// Вспомогательная функция для теста, которая создает массив из рандомных элементов
void arrayRandom(int array[], int size);

// Функция тестов
bool test();
